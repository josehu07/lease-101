[project]
name = "lease_demo"
version = "0.1.0"
description = "Distribute Lease 101 Scripts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["isort>=8.0.1", "ruff>=0.15.17"]
